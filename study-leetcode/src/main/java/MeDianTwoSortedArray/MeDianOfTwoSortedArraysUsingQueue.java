package MeDianTwoSortedArray;

/**
 * @autor yeqiaozhu.
 * @date 2019-05-10
 */
//todo 用大小堆找两个有序数组的中位数
public class MeDianOfTwoSortedArraysUsingQueue {

    /*private final PriorityQueue priorityQueue1 = new PriorityQueue<Integer>((t1, t2) ->
    {
        return t1 - t2;
    }

    );

    private final PriorityQueue priorityQueue2= new PriorityQueue<Integer>((t1,t2)->{
        if (t1 < t2) {
            return 1;
        } else if (t1 == t2) {
            return 0;
        } else {
            return -1;
        }
    });

    public double findMedianSortedArrays(int[] nums1, int[] nums2) {
        Arrays.stream(nums1).forEach(action->{
            priorityQueue1.offer(nums1);
        });

        Arrays.stream(nums2).forEach(action->{
            priorityQueue2.offer(nums2);
        });


    }*/
}
